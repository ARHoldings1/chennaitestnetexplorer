<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Transactions - Chennai Testnet</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="icon" type="image/x-icon" href="/assets/favicon.ico">
    <link rel="shortcut icon" href="/assets/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
</head>
<body>
    <header>
        <!-- ... header content (same as index.ejs) ... -->
    </header>
    
    <main class="container">
        <h2>All Transactions</h2>
        <table>
            <thead>
                <tr>
                    <th>Txn Hash</th>
                    <th>Method</th>
                    <th>Block</th>
                    <th>Age</th>
                    <th>From</th>
                    <th>To</th>
                    <th>Value</th>
                    <th>Txn Fee</th>
                </tr>
            </thead>
            <tbody>
                <% transactions.forEach(tx => { %>
                    <tr>
                        <td><%= tx.hash.substring(0, 16) %>...</td>
                        <td>Transfer</td>
                        <td><%= tx.blockNumber %></td>
                        <td><%= getTimeDifference(tx.timestamp) %></td>
                        <td><%= tx.from.substring(0, 16) %>...</td>
                        <td><%= tx.to.substring(0, 16) %>...</td>
                        <td><%= web3.utils.fromWei(tx.value.toString(), 'ether') %> DOT</td>
                        <td><%= calculateTxnFee(tx) %> DOT</td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </main>

    <footer>
        <!-- ... footer content (same as index.ejs) ... -->
    </footer>

    <script src="/script.js"></script>
</body>
</html>